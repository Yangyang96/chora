# Chora 公开路线图

[English](ROADMAP.md) · [README](README.zh-CN.md)

状态（2026-09-21）：源码已公开的 Developer Alpha。真实多仓库交付技术验收、
`M2_S3_PASS` 和本地 DA-1/O5 准备已完成。源码于 2026-09-10 公开，版本化预发布见
[Releases 页面](https://github.com/Yangyang96/chora/releases)。整个 M2 继续进行；M2-S4-1 已于 2026-09-13 通过技术验收，M2-S4-2 已从必交付范围撤下，不记为 PASS。

Project 长期拥有仓库资源和多个主题 Room；Task 属于一个 Room，并选择本次仓库范围。
空 Project、多 Repo、独立 Room 历史和 Task worktree 已属于当前基础。Chora 拥有
工作界面、持久 Task/Run/Review 状态和执行集成，成熟 Provider 提供模型/工具循环
及基础设施。Spec Coding 是第一个场景。首版仍限定源码启动、Apple Silicon macOS、
已有本地 Git 仓库、用户控制的 Pi、显式 Local Connected / No Sandbox，不是稳定版
或安装包版本。M2-S5-1 已于 2026-09-14 通过技术验收；M2-S5-2 的 Pi 模型选择
已于 2026-09-15 通过技术验收。M2-S5-3 可选应用预览已于 2026-09-16 通过技术验收。
M2-S6 本地应用实现及原生交互验收已完成，正式分发继续暂缓。M2-S6A 原生任务看板 V0
已于 2026-09-19 通过技术验收；M2-S7 Provider 原生 Skills/MCP 配置已于 2026-09-20
通过 Local Connected 范围的技术验收。

已接受的下一步方向是软件 Project 内的调研、方案和文档协作，以及更简洁的执行设置。
与软件无关的通用工作不在范围。M2-S8-1 执行术语已实现；M2-S8-2 可复用执行设置已完成。
M2-S9-1 与 M2-S9-2 已于 2026-09-21 通过技术验收。见[产品决策](docs/project-workflows.zh-CN.md)。

M2 可靠性补充加入了 Docker 实时状态观察、有界 Pi 进度捕获、经过资格验证的容器内浏览器工具，
并在评审中明确展示验收覆盖的局限。全新真实 Agent 样本使用了浏览器能力，但需要评审反馈
才能修复正确性和检查报告问题。代码由 Agent 自行修复；这属于评审驱动的返修，不是首次
自主完成。浏览器可用和所选命令成功不等于任务整体验收通过，也不代表 M2 整体完成。
支持边界见[隔离执行](docs/isolated-local.zh-CN.md)。

| 阶段 | 结果 | 状态 |
| --- | --- | --- |
| M0/M1 | 确定性基础和一次真实隔离本地编码闭环 | 历史维护者边界已验收；M1 私有资产不公开分发。 |
| M2-S1 | 本地 Pi Task worktree、可见工作、Review 和 Resume | 保留技术检查点；最终产品用户验收待通过。 |
| M2-S2-P1/P2/P2A | 请求边界、不可变任务基线、Project/Room 归属和迁移 | 已在发布前集成候选中通过技术验收，历史证据保留。 |
| P3/P4 重设计 | 多仓库 Task、文本及新文件评审、自动检查、大仓库策略和部分恢复 | 真实多仓库连续任务技术验收已通过。 |
| M2-S2 交付 | Task 分支 Commit → Push → GitHub PR → Merge → 安全清理，统一任务状态/进度 | 真实多仓库集成验收及修复已通过，旧 Apply 保留兼容。 |
| M2-S3-0～3 | 结束不需要的结果、Pi 上手/模型来源、维护恢复和公开流程/CI | `M2_S3_PASS`：S3-0～3 技术验收及统一独立审阅已完成。 |
| M2-S5-1 | 原生执行中补充指令，以及关联证据的决策、风险、未知项、人工 Gate、解决和重新打开 | 2026-09-14 通过技术验收；内部证据仅内部保留。 |
| M2-S5-3 | Local Connected 和 Isolated Local 下的按需 Task 应用预览、日志、停止与恢复清理 | 2026-09-16 在 Apple Silicon macOS 上通过技术验收。 |
| M2-S6 | macOS 应用、内置运行时、归属明确的服务生命周期及本地原生交互 | 本地实现/开发包验证完成；2026-09-17 记录原生交互验收。正式分发和签名更新资格验证继续暂缓。 |
| M2-S6A | 原生任务看板 V0：已有 Task、Project/Room 视图、注意事项及有证据的阶段投影 | COMPLETE：S6A-1/2/3 于 2026-09-19 通过技术验收，范围与验证摘要见下文。 |
| M2-S7 | Local Connected 中项目范围的原生 Skills/MCP | 2026-09-20 通过技术验收。 |
| M2-S8 | 两种执行环境与可复用项目设置 | S8-1 与 S8-2 已于 2026-09-20 完成。 |
| M2-S9 | 软件项目内的调研、方案与文档协作 | S9-1 与 S9-2 已于 2026-09-21 通过技术验收。 |
| DA-1 / O5 v2 | 精确本地候选、公开检查、许可证/SBOM、秘密扫描和发布材料 | 本地候选准备已通过；用户最终验收和发布动作独立记录。 |

技术验收于 2026-09-09 冻结，随后完成身份调整及发布修复。公开验证基线为
[提交 `7aaea26`](https://github.com/Yangyang96/chora/commit/7aaea26cbd67c44ce94c70d017c09ca116ab6e6a)，
[Linux/macOS 测试与浏览器 CI](https://github.com/Yangyang96/chora/actions/runs/34440033315)
及[秘密扫描](https://github.com/Yangyang96/chora/actions/runs/34440033375)通过。
完整真实 Pi 和恢复验收记录由维护者另行保留，无凭据 CI 不能替代它们。
skip 和历史不适用项不计为 PASS。此处记录绑定源码的里程碑验收，不声明后续 HEAD
已重新执行全部验收，也不代签用户最终验收。

## 首版公开 Alpha 门槛

连续准备顺序：接收在途工作 → R5/已有 SCM 真实技术验收与修复 → S3-0～3 →
DA-1/O5 本地候选与发布准备 → 用户最终验收 → 用户实际发布。

准备 Goal 启动后，技术阶段不逐项等待人工批准。
`PREPUBLICATION_READY_FOR_USER_ACCEPTANCE` 表示适用发布前技术工作完成，可运行的
验收环境、冻结且已验证的源码和发布操作包均已就绪。`PUBLIC_DEVELOPER_ALPHA_READY`
还需要用户接受准确候选；两个状态都不代表已发布。实际发布由用户执行；验收反馈
产生修改时，重新验证受影响内容并更新候选。

此候选包含完整 Commit/Push/PR/Merge/清理。默认路线为 Task 分支/worktree →
开发与检查 → Review → 逐 Repo Commit → Push → GitHub PR → Merge → 清理。
Apply 保留为可选本地写回及历史兼容，不静默排除 SCM 来缩小验收。交付不隐式推进
用户原 checkout；后续 Task 明确选择逐仓库当前基线，旧历史保持不可变。

M2-S3-1 必须提供应用内 Pi 安装/配置向导，复用已有兼容 Pi，依赖满足时支持点击
安装。展示来源、已验证版本、位置、进度和恢复，使用原生认证并重新检测就绪。
缺少 Node 时提供手动指引；Chora 安装包、自动配置系统环境和自动升级 Agent 不在范围。

开发者应能使用干净源码/manifest 和自己的 Pi 配置，在至少两个 Repo、两个 Room
连续完成有用任务，包括新增文件、准确检查、修复、分组 Review 和完整交付。验证
新基线、旧历史、重启/Resume、部分交付恢复、旧 Apply 兼容、备份恢复清理、schema
限制、错误恢复及键盘/窄窗可用性。复用未变化的真实回归证据。

无凭据 CI 与显式真实 Pi/macOS/GitHub 证据分开，skip 或 UNKNOWN 不计为 PASS。
发布前必须完成干净候选流程、SBOM/许可证和专用源码/历史秘密扫描。
已有目标设置提前只读核验；仅在
发布时才能验证的项目明确列出，不冒充通过。本地 Pi 公开路线不依赖 M1 私有镜像
或维护者 OAuth。

## 已完成的首版可用性：M2-S3

S3-0 结束不需要的未交付结果，保留历史和全部已有 Commit/Push/PR/Merge/Apply
事实，不撤销已交付修改、不自动删除 worktree。部分或不确定操作先对账；归档与
清理分别检查资格。S3-1 补齐已验证的 Pi 上手、实际模型来源及原生配置指引；S3-2
覆盖源码更新、兼容、备份和恢复；S3-3 验证集成公开流程和维护中的 CI。四个子阶段
均已通过技术验收。

M2-S8 执行设置已完成，M2-S9 项目协作已于 2026-09-21 通过技术验收。S7 原生 Skills/MCP、
S6A 任务看板 V0 已完成；S6 本地实现已完成，正式 macOS 分发继续作为独立发布路线。
这些优先级不新增首版 Alpha 验收门槛。

## 已完成的隔离执行与撤下的 profile 验证

M2-S4-1 提供一种 [Isolated Local 环境](docs/isolated-local.zh-CN.md)，已于 2026-09-13
通过技术验收，覆盖公开准备、凭证、执行、检查、Review → Commit → Push → PR → Merge
→ 清理、取消、重启和 Resume。后续模型控制见下文。保留不可变多仓库任务基线、旧
Apply 和隔离 fail-closed 行为。

2026-09-20 的[产品决策](docs/project-workflows.zh-CN.md)将 M2-S4-2 Minimal/Standard
资格验证从已排期交付及 M2 完成要求中撤下，不记为已完成或 PASS。Minimal 仅在有价值
时保留为内部对照配置；Standard 中有用的工具归入默认 Agent 能力。保留历史 ID、策略
绑定与证据。新增预设按真实需求触发，不再承担重建旧矩阵的义务。

## 下一阶段个人工作空间切片

下表是已接受的实施顺序，不承诺日历日期。规划接受不代表功能实现，也不在本次文档
任务中启动实施。新增编号保留全部旧里程碑编号及证据。

| 顺序 | 切片 | 开始条件 | 完成结果 |
| --- | --- | --- | --- |
| 完成 | M2-S8-1：执行术语 | 现有本机与隔离 Pi 路径。 | 新 Task 两种选择统一为 **本机执行 · 无沙箱** / **隔离执行**；定向 API/UI 测试和浏览器 E2E 覆盖选择、确认与风险披露。技术 ID、命令、路径和策略标识符保持不变。 |
| 完成 | M2-S8-2：可复用项目执行设置 | S8-1 术语通过验证。 | 项目默认值、明确任务覆盖和准确配置摘要贯穿执行与重启。 |
| 完成 | M2-S9-1：项目调研与提案评审 | S8 执行设置可用，并选定一个具体项目问题。 | 仓库可选的 Task 产出有来源结论/提案，支持反馈、评审与重开，不制造代码交付。 |
| 完成 | M2-S9-2：文档修订与实现衔接 | S9-1 结果、评审和来源契约已验证。 | 修订并接受项目方案/文档，将该接受版本显式绑定到后续编码 Task。 |
| 进行中 | M3 团队协作与委派 | 优先排在个人 S9 流程之后；委派前验证身份、成员和并发。 | 共享归属、评审、交接、汇总和冲突处理。 |
| 有明确需求时 | M4 后台/远程执行 | 有界场景需要离开前台运行。 | 明确执行位置、取消、恢复、预算和通知。 |

S8/S9 不等待签名 macOS 分发、第二 Runtime、Contexere 或通用 Work App。S6 的签名/
公证、干净机器和签名更新验证继续留在独立发布路线。扩展隔离环境依赖或 Skills/MCP
需要独立需求与验证，不隐含在项目默认设置中。按反馈复核优先级，不要求所有未来
M2 选项全部完成后才能开展任何 M3 工作。

### M2-S8 — 执行设置

复用 S5-2 模型控制、S7 原生能力配置和现有 Pi 本机/隔离路径。Task 显式覆盖优先于
Project 默认值，展示实际环境、模型选择及相关能力状态，详情按需展开。Runtime 默认
模型与观察到的模型区分。不新增 Provider 平台、模型路由器或策略矩阵。S8-1 的界面和使用文档采用一致的执行环境名称。

按 Task 和执行记录冻结实际配置及来源/版本。Project 修改不重新绑定已有任务、
重试或恢复会话。显式切换沿用受支持后继执行规则，不扩大规则或修改旧 Attempt。
本机选择仍需要披露确认；隔离不可用时，不自动切到宿主、另一模型或额外凭证。

完成标准：定向术语/配置测试、相关浏览器 E2E，以及两种已有执行环境受影响的真实 Pi
验证。证明继承/覆盖、项目间隔离、不可用选择、重启、本机确认和无宿主回退。
S8-1 不要求迁移、重开或验证旧 Minimal/Standard 历史，也不新增兼容适配层；内部
对照夹具可以保留，但不构成产品承诺。

### M2-S9 — 调研、方案与文档

沿用 Task/Run/Result 机制，根据工作选择资源、结果和检查。S9-1 接受明确提供的软件
项目问题和材料，不要求 Git 仓库或代码改动。使用已授权 Provider 能力，不新增连接器
目录或强制任务类型表单。展示来源、未知项、反馈和可评审结论。Agent 完成与人工接受
区分，两者均不自动启动另一个任务。

S9-2 提供项目方案或 Markdown 文档的有界修订/评审流程，复用 Artifact、Decision 和
上下文机制，保留来源/版本身份与评审历史。后续编码 Task 显式选择已接受版本作为冻结
输入，之后文档编辑不能改变这一绑定。材料留在所属 Room，直到被明确引用或共享。
仓库中的文档编辑继续遵守仓库检查和交付；接受 Room 产物不授权仓库写入或发布。

已实现流程支持粘贴 1～16 份材料，冻结快照总量必须满足持久化元数据边界；Agent 完整
Markdown 结果限制为 64 KiB。保存、修订和接受是彼此独立的显式动作。接受会创建精确的
Room 修订；后续同一 Room 的 Task 必须显式选择该接受版本。文档变化不会更新已有 Task，
任何文档操作都不会创建 SCM 交付、发布权限或自动启动后续实现 Task。

每个子切片的完成标准：有用的端到端工作、反馈/重试及重启/重开，准确保留来源和缺失
证据状态。包括 API/持久化测试、相关浏览器 E2E 和真实 Pi 项目流程。扩展 Task Board
对非代码结果的投影，不虚构 SCM 交付阶段。回归编码 Diff/检查/Review/SCM；资源范围、
执行权限和已接受历史不得静默扩大。通用办公、实时多人编辑器、工作流引擎和新上下文
基础设施不在 S9 范围。

S9-1 与 S9-2 于 2026-09-21 在 Apple Silicon macOS 上通过技术验收。API/持久化与
浏览器测试覆盖修订评审、陈旧来源拒绝、显式选版与冻结衔接。真实 Local Pi 完成两轮有来源 Markdown，
包括拒绝、反馈和成功重试；后续编码 Task 使用显式选择的已接受版本产出真实仓库 Diff，
之后接受新文档版本并重启服务，其冻结输入保持完全不变。隔离执行目前只有
自包含自动化覆盖，尚未进行真实模型隔离资格验证。

M2-S5-1 验收已覆盖两个完成的子切片。区分 Agent 提案、系统观测和用户记录，保留证据、版本
及解决历史，允许空/未评估结果。只有必要且有授权依据的 Gate 才阻塞工作，回答
不能重放过期执行。不另建模型循环、不依赖 Contexere。保留这些验收边界，不改变
P3/P4 或首版验收。

## 已完成的原生任务看板 V0（M2-S6A）

状态：**COMPLETE**，2026-09-19。[英文实施方案](docs/task-board-plan.md)及其
[中文译文](docs/task-board-plan.zh-CN.md)保留范围、生命周期映射、API 契约、
B01-B27 验收矩阵与验证摘要。使用方式见[操作指南](docs/task-board.zh-CN.md)。

保留 Project -> Room -> Task 归属。卡片就是已有 Task，不新增 Issue，也不按 Run/仓库
复制任务。Project 增加任务视图及看板/列表切换、Room/仓库筛选，共用 Room 范围视图和
需要处理入口。原有 New Task 继续直达；卡片动作进入已有归属页面，重新读取状态后仍由
原有授权与确认规则决定操作。

采用 **准备中 | 处理中 | 待审阅 | 交付中 | 已结束** 的派生阶段，不新增可编辑状态机。
保留准确子状态与结束类型：接受不等于交付，取消 Run 不一定结束 Task，无变更结束不是
人工接受，部分关闭与清理不等于合并。未知/矛盾证据保留在单独的待核对分组，归档继续
作为独立可见性/只读机制。

| 切片 | 结果 | 状态 |
| --- | --- | --- |
| M2-S6A-1 | 共用的纯阶段/注意事项/结果投影、分路径事实、有界只读 API 及映射/API 测试 | COMPLETE |
| M2-S6A-2 | Project/Room 看板/列表、筛选、安全深链接、新鲜度、可访问性及页面切换连续性测试 | COMPLETE |
| M2-S6A-3 | 集成回归、本地 macOS 验证、实际可用性/性能观测及使用文档 | COMPLETE |

V0 不含 Backlog 草稿、手动优先级/排序、拖拽改状态、卡片内/批量写操作、分配、依赖调度、
外部看板同步或新模型循环。看板读取不调用模型，不创建或启动工作。必须验证从 Run 切到
看板不会让已授权验证停滞；必要修复限于现有执行/服务归属，不扩张为 M4 调度器。
保持 S5 预览与 S6 服务/恢复行为。开始 V0 不依赖签名分发，本切片不授权 Tag、Release
或安装包。

V1 轻量草稿/排序在 M3 前后按需求触发，记录与执行授权分离，明确转为 Task 的关联。
M3 在团队授权/并发基础上增加认领、交接与委派；M4 再增加排队/后台/远程、预算和通知。
这些不是 V0 的隐含工作。S6A 后缀保留既有 S6/S7 里程碑编号。

## M3 首个本地切片：有界研究委派

首个实现切片是[本地 Agent 委派](docs/local-delegation.zh-CN.md)：一个人在一台 Mac 上明确
启动包含一至四项材料型调研／文档分工的固定计划。子任务继承父任务冻结的输入和执行
设置，顺序执行，保留正常 Task／Run／结果身份。父任务汇集有来源的结果供人工评审，
不会自动接受结果，也不授权 SCM 交付。停止、重启与重复命令处理均属于该切片。
后续的来源绑定计划导入允许父 Agent 提出固定分工；明确启动时会重新核对当前结果，
冻结来源而不接受结果。专用调研规划增加一次明确启动：事前持久授权、一次规划 Attempt，
再自动导入通过校验的计划。当前实现与验证进行中。

这是 M3 内的有界起点，不声称多人成员／撤销、并行执行或编码整合已经
完成。M4 仍按实际需求启动。

## 后续方向与需求触发

- M3：项目内多个主题 Room 的多人参与、成员/撤销、并发评审和交接；随后支持
  评审后共享成果和委派；复用当前多仓库 Project/Room/Task 基础，不将其延期到 M3。
- M4：离开前台继续工作，按真实需求选择本机后台或远端，提供取消、恢复、预算、通知。
- M5：共享 SCM/CI 与交付策略、可观察发布/回滚，以及已验证的软件项目工作界面；
  与软件无关的通用应用不在范围。
- M6：企业规模治理、可靠性和扩展生态。

这些是优先级，不是无条件串行依赖。个人异步或基础 PR/CI 可由有界需求提前激活。
第二 Runtime、搜索、内嵌界面和 Contexere 必须先有重复使用摩擦或能力/上下文瓶颈，
不为它们预建通用平台。各切片逐步承担安全、数据持久性和兼容性；初期反馈可人工
收集，不要求遥测平台。

### Runtime 自主管理模型能力发现

M2-S5-2 不要求 Chora 维护手工模型白名单。每个 Runtime 自己负责能力发现与校验。Chora 可以缓存仅含身份信息的目录用于展示，但启动时必须再次向 Runtime 校验选择。模型变更只影响新的 Attempt；旧 Attempt 的请求和实际 Provider/模型来源保持不可变。Runtime 移除模型或认证失败时必须 fail-closed，不能静默替换。

Local Connected 从选定的 Pi 可执行文件及配置发现模型。Isolated Local 在无凭据、
无网络条件下读取已准备镜像内 Pi 的内置目录；目录不代表所有 Provider 都已认证，
当前隔离凭据投影仍仅支持 DeepSeek。新任务可使用 Runtime 默认值或显式选择模型。
重试默认继承上一次 Attempt 的选择，也可改选；界面分别展示请求模型与观测模型。
切换执行模式保留模型身份，并要求目标 Runtime 支持该模型。Pi CLI 无法精确表达的
歧义模型引用会被拒绝，不会解析成另一个别名模型。

模型选择验收覆盖 Local Connected 中两个真实 DeepSeek 模型、Isolated Local 中
Flash/Pro 执行、Workbench 重启、新隔离 Attempt 改选模型，以及不可变的请求与
观测模型历史。
该结论限定 Apple Silicon macOS 上当前 Pi 集成，不代表其他 Runtime 或目录中
所有 Provider 均已验收。

### 按需应用预览

M2-S5-3 在 Task 最新运行中增加默认折叠的可选[应用预览](docs/app-preview.zh-CN.md)。
代码、diff、检查和 Review 仍是主要验收方式，运行应用不是验收或交付的必要条件。
用户主动配置和启动每个可写 Task 仓库的单个 Web 应用，打开本机回环 URL、
查看有大小上限的日志、停止应用并清理资源。

Local Connected 在校验过的 Task 工作区执行，保持 No Sandbox。
Isolated Local 使用已准备镜像中的独立应用容器、私有源码快照和一个本机回环端口；
Agent 容器策略不变，不回退到主机。切换页面后应用继续运行；后续 Attempt 执行、
工作区清理及正常退出 Workbench 前会停止预览。恢复时校验持久化归属，不自动重启。
依赖不会自动安装；隔离预览需要重新启动才能反映较新的源码。

预览验收覆盖真实浏览器操作和已准备隔离镜像中的无凭据应用，包括启动、页面切换后的
持续运行、日志、停止、工作区清理、正常退出、持久化恢复，以及代码评审状态不受影响。
完整测试、竞态检查、vet 和标准 E2E 均在 Apple Silicon macOS 上通过。
此验收不代表其他平台、任意应用镜像或多服务编排已获支持。

### macOS 应用实现

M2-S6 提供菜单栏应用及可重复的 Apple Silicon 打包入口，包含 Node.js/Pi、原生
Pi 认证、归属明确的服务生命周期、仅空闲时备份恢复、源码数据接管与校验后手动更新。
使用约定见 [macOS 应用](docs/macos-application.zh-CN.md)。

本地实现、开发包验证与正式分发分别记录。Developer ID 签名、公证、干净机器首次
启动及签名更新/恢复资格验证仍是发布门禁；此实现不声明已提供公开安装包或发布版本。

本地原生交互于 2026-09-17 完成验收：首次初始化、Provider 选择与 Pi 凭据保存、
认证取消、关闭浏览器标签后服务保留、菜单重新打开 Workbench、取消退出及确认后
干净退出。重名认证选项保持各自 Provider 身份。正式分发继续暂缓；此结果不代表
签名安装包或签名更新完整流程已通过资格验证。
