# Chora 公开路线图

[English](ROADMAP.md) · [README](README.zh-CN.md)

状态（2026-09-09）：公开前 Developer Alpha。多仓库基础和 Task 分支交付已实现，
并有专项验证；真实连续使用整体验收及发布准备仍待完成。本页不代表已发布，
计划项也不是已经实现的承诺。

Project 长期拥有仓库资源和多个主题 Room；Task 属于一个 Room，并选择本次仓库范围。
空 Project、多 Repo、独立 Room 历史和 Task worktree 已属于当前基础。Chora 拥有
工作界面、持久 Task/Run/Review 状态和执行集成，成熟 Provider 提供模型/工具循环
及基础设施。Spec Coding 是第一个场景。首版仍限定源码启动、Apple Silicon macOS、
已有本地 Git 仓库、用户控制的 Pi、显式 Local Connected / No Sandbox，不是稳定版
或安装包版本。

| 阶段 | 结果 | 状态 |
| --- | --- | --- |
| M0/M1 | 确定性基础和一次真实隔离本地编码闭环 | 历史维护者边界已验收；M1 私有资产不公开分发。 |
| M2-S1 | 本地 Pi Task worktree、可见工作、Review 和 Resume | 保留技术检查点；最终产品用户验收待通过。 |
| M2-S2-P1/P2/P2A | 请求边界、不可变任务基线、Project/Room 归属和迁移 | 已实现，保留技术证据；需验证当前集成候选。 |
| P3/P4 重设计 | 多仓库 Task、文本及新文件评审、自动检查、大仓库策略和部分恢复 | 已实现；真实多仓库连续验收尚待收口。 |
| M2-S2 交付 | Task 分支 Commit → Push → GitHub PR → Merge → 安全清理，统一任务状态/进度 | 已实现且有专项检查；完成真实集成验收和必要修复。 |
| M2-S3-0～3 | 结束不需要的结果、Pi 上手/模型来源、维护恢复和公开流程/CI | 下一研发范围；补齐剩余缺口并完成集成验收。 |
| DA-1 / O5 v2 | 精确本地候选、公开检查、许可证/SBOM、秘密扫描和发布材料 | 准备尚未完成；候选门禁与用户最终验收分开。 |

## 首版公开 Alpha 门槛

连续准备顺序：接收在途工作 → R5/已有 SCM 真实技术验收与修复 → S3-0～3 →
DA-1/O5 本地候选与发布准备 → 用户最终验收 → 用户实际发布。

准备 Goal 启动后，技术阶段不逐项等待人工批准。
`PREPUBLICATION_READY_FOR_USER_ACCEPTANCE` 表示适用发布前技术工作完成，可运行的
验收环境、冻结且已验证的源码和发布操作包均已就绪。`PUBLIC_DEVELOPER_ALPHA_READY`
还需要用户接受准确候选；两个状态都不代表已发布。实际发布由用户执行；验收反馈
产生修改时，重新验证受影响内容并更新候选。

此候选包含完整 Commit/Push/PR/Merge/清理。默认路线为 Task 分支/worktree →
开发与检查 → Review → 逐 Repo Commit → Push → GitHub PR → Merge → 清理。
Apply 保留为可选本地写回及历史兼容，不静默排除 SCM 来缩小验收。交付不隐式推进
用户原 checkout；后续 Task 明确选择逐仓库当前基线，旧历史保持不可变。

M2-S3-1 必须提供应用内 Pi 安装/配置向导，复用已有兼容 Pi，依赖满足时支持点击
安装。展示来源、已验证版本、位置、进度和恢复，使用原生认证并重新检测就绪。
缺少 Node 时提供手动指引；Chora 安装包、自动配置系统环境和自动升级 Agent 不在范围。

开发者应能使用干净源码/manifest 和自己的 Pi 配置，在至少两个 Repo、两个 Room
连续完成有用任务，包括新增文件、准确检查、修复、分组 Review 和完整交付。验证
新基线、旧历史、重启/Resume、部分交付恢复、旧 Apply 兼容、备份恢复清理、schema
限制、错误恢复及键盘/窄窗可用性。复用未变化的真实回归证据。

无凭据 CI 与显式真实 Pi/macOS/GitHub 证据分开，skip 或 UNKNOWN 不计为 PASS。
最终干净候选流程、SBOM/许可证和专用源码/历史秘密扫描按
[发布流程](docs/release-process.zh-CN.md)执行。已有目标设置提前只读核验；仅在
发布时才能验证的项目明确列出，不冒充通过。本地 Pi 公开路线不依赖 M1 私有镜像
或维护者 OAuth。

## 剩余首版可用性工作

S3-0 结束不需要的未交付结果，保留历史和全部已有 Commit/Push/PR/Merge/Apply
事实，不撤销已交付修改、不自动删除 worktree。部分或不确定操作先对账；归档与
清理分别检查资格。S3-1 补齐已验证的 Pi 上手、实际模型来源及原生配置指引；S3-2
覆盖源码更新、兼容、备份和恢复；S3-3 验证集成公开流程和维护中的 CI。复用已实现
且验证准确的行为。

后续 M2 包括完整模型控制、执行中补充指令、本地运行/预览、原生 Skills/MCP 和
安装包生命周期；M3 负责 Agent 委派、汇总及冲突，M4 增加后台/远程执行。保持下文
优先级，不新增首版 Alpha 门槛。

## 首次公开 Alpha 后的第一个功能里程碑：M2-S4

计划，尚未开始。M2-S4-1 将一种完整 Isolated Local 模式接入 Workbench，复用
已采用 Sandbox，覆盖公开环境准备、认证、执行、检查、Review/Apply、取消、清理
和 Resume。M2-S4-2 在工具/依赖、网络、资源策略有清晰且经过验证的区别时开放
Minimal/Standard，可先发布一种已验证模式。隔离失败绝不回退宿主执行。

默认顺序：M2-S3 → 公开候选检查 → 首次 Alpha 发布 → M2-S4。不新增首版 Alpha
要求，不等待 M3/M4。复用已有隔离证据和当前 Project/Room/Task 契约，不重建
Runtime 或 Sandbox。这是后续版本规划，不代表当前不可用的执行选项已经开放。

## 开源后优先级排期

默认交付优先级，均为 PLANNED_NOT_STARTED，暂无日历日期：

1. M2-S4：一种可用隔离模式，Minimal/Standard 验证后开放。
2. M2-S5-1：S5-1a 原生执行中补充指令；S5-1b 关联证据的决策、风险、未知项，
   必要人工 Gate、解决和重新打开。
3. M2-S5-2：显式选择受支持模型配置，与执行模式区分。
4. M2-S5-3：本地应用启动/预览、日志、停止和清理。
5. M2-S6：一种受支持 macOS 安装包、兼容升级、恢复和卸载。
6. M2-S7：显式配置 Provider 原生 Skills/MCP，查看实际能力。
7. M3：团队授权基础具备后的 Agent 委派、汇总及冲突处理；后台/远程继续归 M4。

此处为原候选确定排序，不新增首版 Alpha 门槛、不修改 P3/P4。S4-2 可延期而不阻塞
S5，M3 不必等全部 M2 完成。发布检查点依据实际反馈细化范围、记录优先级调整；
列入排期不代表已实现或自动授权执行。

M2-S5-1 需两个子切片通过。区分 Agent 提案、系统观测和用户记录，保留证据、版本
及解决历史，允许空/未评估结果。只有必要且有授权依据的 Gate 才阻塞工作，回答
不能重放过期执行。不另建模型循环、不依赖 Contexere。这是开源后规划，不代表
当前能力，也不改变 P3/P4 或首版验收。

## 后续方向与需求触发

- M3：项目内多个主题 Room 的多人参与、成员/撤销、并发评审和交接；随后支持
  评审后共享成果和委派；复用当前多仓库 Project/Room/Task 基础，不将其延期到 M3。
- M4：离开前台继续工作，按真实需求选择本机后台或远端，提供取消、恢复、预算、通知。
- M5：共享 SCM/CI 与交付策略、可观察发布/回滚、已验证应用扩展。
- M6：企业规模治理、可靠性和扩展生态。

这些是优先级，不是无条件串行依赖。个人异步或基础 PR/CI 可由有界需求提前激活。
第二 Runtime、搜索、内嵌界面和 Contexere 必须先有重复使用摩擦或能力/上下文瓶颈，
不为它们预建通用平台。各切片逐步承担安全、数据持久性和兼容性；初期反馈可人工
收集，不要求遥测平台。
